- clean: ['~']

- link:
    ~/.config:    config/
    ~/.gitconfig: .gitconfig
    ~/.pryrc:     pryrc
    ~/.tmux.conf: tmux.conf
    ~/.vimrc:     vimrc
    ~/.vim:       vim/
    ~/.zshrc:     zshrc
    ~/.zsh:       zsh/

- shell:
  - [git pull && git submodule update --init --recursive, Installing/updating submodules]
