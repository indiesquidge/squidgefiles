- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.vim:   vim/
    ~/.zshrc: zshrc
    ~/.zsh:   zsh/

- shell:
  - [git submodule update --init --recursive, Installing/updating submodules]
